# SDAR Integration

This additive patch introduces only a telemetry sink contract document and does not modify runtime authority tables. The actual locked SDAR checkout was absent in the task environment, so apply-check against SHA 27fddc... is environment-blocked.
