# Capacity Report

Configurable WAL high-water limit is enforced before append. When exceeded, ingestion returns a retryable response. Production throughput and retention sizing require target hardware and live ClickHouse.
