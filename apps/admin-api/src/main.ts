import http from "node:http";import {loadConfig} from "../../../packages/telemetry-config/src/index.js";const c=loadConfig();const state={sources:[],routes:[],targets:[],revisions:[],jobs:[],dlq:[],qualityIssues:[],audit:[]};http.createServer((req:any,res:any)=>{res.setHeader("content-type","application/json");const role=req.headers["x-role"]??"viewer";if(req.method!=="GET"&&role!=="admin"){res.statusCode=403;return res.end(JSON.stringify({error:"admin role required"}))}const key=(req.url??"/").split('/').filter(Boolean).at(-1) as keyof typeof state;if(req.method==="GET"&&key in state)return res.end(JSON.stringify(state[key]));if(req.method==="GET"&&req.url==="/health")return res.end(JSON.stringify({status:"ok"}));res.statusCode=404;res.end(JSON.stringify({error:"not found"}))}).listen(c.adminPort,()=>console.log(`admin-api:${c.adminPort}`));
