import test from "node:test";import assert from "node:assert/strict";import {assertBatch} from "../../packages/telemetry-contracts/src/index.js";import {V13_FAMILIES,V14_FAMILIES} from "../../packages/telemetry-types/src/index.js";test("batch contract",()=>assert.doesNotThrow(()=>assertBatch({schemaVersion:"1.0",sourceId:"s",sourceType:"sdar-v1.3-outbox",batchId:"b",records:[{sourceRecordId:"r",recordFamily:"sdar.runtime.task",occurredAt:new Date().toISOString(),tenantId:"t",payload:{}}]})));test("v13 families",()=>assert.equal(V13_FAMILIES.length,16));test("v14 families",()=>assert.equal(V14_FAMILIES.length,8));
